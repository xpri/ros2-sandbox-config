# ROS2 cheat sheet :D

\>source /opt/ros/jazzy/setup.bash
\# Loads base ROS2 installation into the shell. Allows you to use ros2 commands. DO THIS ANYWHERE SINCE IT ONLY APPLIES TO CURRENT SESSION.

\>source \~/ROS2\_Projects/application\_ws/install/setup.bash
\# Loads my local workspace onto the path. Tells compiler where to find the packages.
